# Data model

_Placeholder — filled in during the schema/data-wiring phase, once `mock-data.ts` is replaced by real Drizzle queries and the `playerGame`/`oauthAccount` tables land. See `src/lib/server/db/schema.ts` for the current source of truth in the meantime._
