# Auth

_Placeholder — filled in during the auth phase, once real `/login`/`/register` pages and OIDC provider support land. Session mechanics already exist in `src/lib/server/auth.ts` and `src/hooks.server.ts` (cookie-based, no UI yet)._
