<!-- The load function always redirects; this only exists so the route has a component. -->
<main class="flex min-h-[60vh] items-center justify-center">
	<p class="text-[var(--text-muted)]">Logging out…</p>
</main>
